# Воспроизведение результатов Serial (последовательная версия)

## Сборка

```bash
g++ -std=c++11 serial_solve.cpp -O2 -o serial_solve
```

## Запуск экспериментов

Эта версия программы принимает участие только в сравнениях с другими и отдельно не запускается. 
